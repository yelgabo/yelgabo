# Candy board helper

Keep the matrix input form so the user can change the visualization. No game logic means no matching, clearing, gravity, swapping, or simulation. It does not mean removing the input form. Preserve exact input values and their order.
